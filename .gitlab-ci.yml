default:
  image:
    name: ubuntu:latest

stages:
  - test
  - release

unit-test-job:
  stage: test
  script:
    - sbt clean test

deploy-job:
  stage: release
  environment: production
  script:
    - echo "TODO run Semantic Release"
